name: Build and Deploy Data

on:
  push:
    branches: [main]
    paths:
      - "groups.json"
      - "iOS/**"
      - "mac/**"
      - "osx/**"
      - "scripts/**"
      - ".github/workflows/build-release.yml"

  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout data repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Download previous build
        id: previous
        run: |
          mkdir -p previous
          if gh release download --pattern "ent.db" --pattern "kv.tar.gz" -D previous 2>/dev/null; then
            tar -xzf previous/kv.tar.gz -C previous
            rm previous/kv.tar.gz
            echo "found=true" >> "$GITHUB_OUTPUT"
          else
            echo "found=false" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Build artifacts
        timeout-minutes: 50
        run: |
          ARGS="-o build-output"
          if [ "${{ steps.previous.outputs.found }}" = "true" ]; then
            ARGS="$ARGS --previous previous"
          fi
          python -u scripts/build.py $ARGS

      - name: Create release
        run: |
          TAG="$(date -u +%Y-%m-%d)"
          if gh release view "$TAG" >/dev/null 2>&1; then
            SEQ=2
            while gh release view "${TAG}-${SEQ}" >/dev/null 2>&1; do
              SEQ=$((SEQ + 1))
            done
            TAG="${TAG}-${SEQ}"
          fi

          cp build-output/ent.db ent.db
          cd build-output && tar -czf ../kv.tar.gz --exclude=ent.db . && cd ..
          gh release create "$TAG" \
            --title "Data Build $TAG" \
            --notes "Auto-built from latest data." \
            --latest \
            ent.db kv.tar.gz
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./build-output

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

